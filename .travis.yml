language: php
dist: trusty
php:
    - 5.4
    - 5.5
    - 5.6
    - 7.0
    - 7.1
    - 7.2
    - 7.3
    - 7.4

before_install:
    - travis_retry composer self-update
    - phpenv config-rm xdebug.ini
    
install:
    - travis_retry composer install

notifications:
    email:
        on_success: false

